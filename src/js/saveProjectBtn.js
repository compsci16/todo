const saveProjectBtn = document.getElementById('saveProjectBtn'); 

function extractTasks(){
    
}

/**
 * @summary - Defines the workflow following Save Project click. 
 */
function saveProjectWorkflow(){
// Extract all tasks from the modal form
}

export default function saveProjectBtn(){
    saveProjectBtn.addEventListener('click', saveProjectWorkflow);
}
